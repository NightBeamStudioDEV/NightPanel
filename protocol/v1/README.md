# Protocol 1 compatibility fixtures

These files are public, non-secret examples used to verify NightPanel clients and compatible server bridges. Values are synthetic and must never be replaced with production tokens, player databases, server addresses, or signing material.

